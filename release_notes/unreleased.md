**Unreleased**

* Ported the Venafi connector to the Splunk SOAR SDK with all seven actions implemented: test connectivity, list policies, list certificates, get certificate, create certificate, renew certificate, and revoke certificate.
* Carried over the configurable asset-level OAuth scope (blank uses the default scope). [ESPM-5451]
